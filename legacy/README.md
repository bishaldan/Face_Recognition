# Legacy Prototype

The original Tkinter prototype is preserved in this folder as historical reference:

- `main_tkinter.py`
- `util_tkinter.py`

The v2 application is now implemented as a FastAPI web platform and does not evolve this legacy code in place.

